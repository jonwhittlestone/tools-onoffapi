{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run onoffapi",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "env": {
                "API_KEY": "devkey",
                "PORT": "8080"
            }
        }
    ]
}
